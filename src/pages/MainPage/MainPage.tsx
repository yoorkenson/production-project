import React from 'react';
import {Counter} from "../../components/Counter";

const MainPage = () => {
    return (
        <div>
            MAIN PAGE
            <Counter />
        </div>
    );
};

export default MainPage;